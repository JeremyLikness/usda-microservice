version: '2'
services:
    seed:
        build: 
            context: .
            dockerfile: Dockerfile-seed
        volumes:
            - /seed
    db:
        image: mongo
        volumes_from: 
            - seed
        command: ["/seed/seed.sh"]
    descriptions:
        build: ./descriptions
        links: 
            - db
        ports:
            - "3000:3000"
    web:
        build: ./usdaweb 
        ports:
            - "80:80"
        depends_on:
            - descriptions